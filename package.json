{
  "name": "feed_discapacidad",
  "version": "1.0.0",
  "description": "Generador de feed para Bluesky",
  "main": "src/app.js",
  "scripts": {
    "start": "PORT=3000 node src/app.js",
    "test:unit": "jest --config config/jest.config.js --runInBand --testPathPattern=tests/unit",
    "test:integration": "jest --config config/jest.config.js --runInBand --testPathPattern=tests/integration",
    "test:e2e": "cypress run --config-file config/cypress.config.js",
    "test:ci": "npm run test:unit && npm run test:integration && npm run test:e2e"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "cypress": "^12.17.4",
    "jest": "^29.0.0",
    "supertest": "^7.1.3"
  },
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
